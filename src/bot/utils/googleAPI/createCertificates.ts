import { Aluno } from "../../typings/talks";
// TODO: Criar um método que para uma determinada lista de alunos e um titulo, crie um arquivo contendo os certificados de cada aluno e salve isso em um documento do google Drive.
async function createCertificate(titulo: string, listaAlunos: Aluno[]) {
    return "Not implemented yet";
}